<!DOCTYPE html>
<html data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/kandidat.css">
  <title>Detalji o kandidatu</title>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <img src="/images/profile-icon-png-910.png" alt="Profile Icon" style="width: 40px; height: 40px; margin-right: 10px;">
      <!--   menadzer.ime  menadzer.prezime -->
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/kreirajKonkurs">Kreiranje i upravljanje Konkursa</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin">Pregled i upravljanje kandidata</a></li>
          <li class="nav-item"><a class="nav-link" href="/chat_hr">Razgovor sa kandidatima</a></li>
          <li class="nav-item"><a class="nav-link" href="/">Odjavi se</a></li>

        </ul>
      </div>
    </div>
  </nav>


  <div class="content-wrapper">
    <!-- Kartica s osnovnim podacima -->
    <div class="card">
      <h4 class="card-title">Podaci o kandidatu</h4>
      <p><strong>Ime:</strong> <%= kandidat.kandidat_ime %></p>
      <p><strong>Prezime:</strong> <%= kandidat.kandidat_prezime %></p>
      <p><strong>Email:</strong> <%= kandidat.kandidat_email %></p>
      <p><strong>Grad:</strong> <%= kandidat.kandidat_grad %></p>
      <% if (kandidat.kandidat_cv) { %>
        <p><strong>CV:</strong> 
          <a href="/<%= kandidat.kandidat_cv %>" target="_blank" class="btn btn-sm btn-primary">Preuzmi CV</a>
        </p>
      <% } %>
      <p><strong>Strani jezik:</strong> <%= kandidat.kandidat_jezik %></p>
      <p><strong>Kandidat iskustvo:</strong> <%= kandidat.kandidat_iskustvo %></p>
      <p><strong>Kandidat ocjena:</strong> <%= ocjene.ocjena %></p>
      <p><strong>Kandidat komentar:</strong> <%= ocjene.komentar %></p>

      <p><strong>Vozačka dozvola:</strong> <%= kandidat.kandidat_vozacka ? "Da" : "Ne" %></p>
      <p><strong>Pozicija:</strong> 
        <% if (kandidat.junior) { %>Junior <% } %>
        <% if (kandidat.medior) { %>Medior <% } %>
        <% if (kandidat.senior) { %>Senior <% } %>
      </p>
      <p><strong>Generiši automatski PDF o kandidatu:</strong><a href="/pdf/<%= konkursID %>/<%= kandidatID %>" class="btn btn-sm btn-primary">Preuzmi PDF Izvještaj</a>
      </p>
    </div>
  
    <!-- Forma za ocjenjivanje -->
    <div class="form">
      <h4 class="card-title">Ocjenjivanje kandidata</h4>
      <form action="/kandidat/<%= konkursID %>/<%= kandidatID %>" method="POST">
        <div class="mb-3">
          <label for="ocjena" class="form-label">Ocjena (1-5):</label>
          <input type="number" class="form-control" id="ocjena" name="ocjena" min="1" max="5" required>
        </div>
        <div class="mb-3">
          <label for="komentar" class="form-label">Komentar:</label>
          <textarea class="form-control" id="komentar" name="komentar" rows="3"></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Dodaj ocjenu i komentar</button>
      </form>
    </div>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>